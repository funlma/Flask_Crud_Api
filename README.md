# Flask, CRUD et Restful API 

Il faut d'abord creer un environement de dev

## Creation de l'environement de developpement

$ py -m venv .

$ cd Scripts

$ activate

## Installation des dependences

$ pip install flask

$ pip install pymongo
